{
  "name": "@otto-assistant/opencode-cursor-oauth",
  "version": "3.0.0-beta.1",
  "description": "Cursor private models in OpenCode 2 — native OAuth, live catalog discovery, images, tools, and agent-grade streaming.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "node scripts/clean.mjs && tsc -p tsconfig.json && node scripts/copy-runtime.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test test/*.test.ts",
    "test:package": "node scripts/check-package.mjs",
    "test:v2-loader": "node scripts/smoke-opencode-v2.mjs",
    "verify": "npm test && npm run typecheck && npm run build && npm run test:package && npm run test:v2-loader",
    "prepublishOnly": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/otto-assistant/opencode-cursor.git"
  },
  "homepage": "https://www.npmjs.com/package/@otto-assistant/opencode-cursor-oauth",
  "bugs": {
    "url": "https://github.com/otto-assistant/opencode-cursor/issues"
  },
  "keywords": [
    "opencode",
    "opencode-plugin",
    "cursor",
    "cursor-pro",
    "oauth",
    "ai",
    "llm",
    "streaming",
    "agent"
  ],
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@ai-sdk/provider": "3.0.8",
    "@bufbuild/protobuf": "^2.0.0",
    "@opencode-ai/plugin": "0.0.0-beta-17639",
    "@opencode-ai/schema": "0.0.0-beta-17639"
  },
  "devDependencies": {
    "@opencode-ai/cli": "0.0.0-beta-17639",
    "@types/bun": "^1.3.11",
    "typescript": "^5.9.3"
  },
  "trustedDependencies": [
    "@opencode-ai/cli"
  ]
}
